{% extends 'base.html.twig' %}

{% block title %}Auteur{% endblock %}

{% block body %}
    <h1>Auteur</h1>

    <table class="table">
        <tbody>
            <tr>
                <th><h3>Id :</h3></th>
                <td>{{ auteur.id }}</td>
            </tr>
            <tr>
                <th><h3>Nom :</h3></th>
                <td>{{ auteur.nom }}</td>
            </tr>
            <tr>
                <th><h3>Prenom :</h3></th>
                <td>{{ auteur.prenom }}</td>
            </tr>
            <tr>
                <th><h3>Description :</h3></th>
                <td>{{ auteur.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_auteur_index') }}"><h3>back to list</h3></a>

    <a href="{{ path('admin_auteur_edit', {'id': auteur.id}) }}">edit</a>

    {{ include('admin/auteur/_delete_form.html.twig') }} 
{% endblock %}
